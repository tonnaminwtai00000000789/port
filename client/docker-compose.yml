services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3367:3367"
    environment:
      - NODE_ENV=production
      - PORT=3367
      - API_URL=/api
      - INTERNAL_API_URL=http://backend:3763
    depends_on:
      - backend
    networks:
      - portfolio-network

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3763:3763"
    environment:
      - NODE_ENV=production
      - PORT=3763
      - DATABASE_URL=postgresql://postgres:fnckpuehkkqhxivyyoagfuck999@db.fnckpuehkkqhxivyyoag.supabase.co:5432/postgres
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
